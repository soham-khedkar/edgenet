# ============================================
# EdgeNet Backend - Production Configuration
# Configure in your hosting platform (Railway/Render/Vercel)
# ============================================

# Server Configuration
PORT=4000
NODE_ENV=production

# Supabase Database Configuration
# Get these from: Supabase Dashboard → Project Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Frontend URL (for CORS)
# Your deployed frontend domain
FRONTEND_URL=https://your-frontend-domain.com

# Optional: Additional CORS origins (comma-separated)
# ADDITIONAL_CORS_ORIGINS=https://app.example.com,https://admin.example.com

# Router Configuration (managed via web UI - optional reference)
# These are stored in Supabase via the setup page
# ROUTER_IP=auto-configured
# ROUTER_USERNAME=auto-configured
# ROUTER_PASSWORD=auto-configured
